<!DOCTYPE html>
<html lang="hu">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link rel="me" href="https://github.com/gyula-ny" />
    <link rel="me" href="https://www.instagram.com/gyula_ny/" />
    <link rel="webmention" href="https://webmention.io/gzn.hu/webmention" />
    <link rel="pingback" href="https://webmention.io/gzn.hu/xmlrpc" />
    <title>{{ title }}</title>
    <style type="text/css">
    body{line-height:1.6;font-size:1.3rem;color:#444;}
    h1,h2,h3{line-height:1.2}
    </style>

  <style>
  body {
    padding: 0;
    margin: 0;
  }
  header nav a { display: block; margin: 0; }
  header {
    margin: 0;
    padding: 0;
    min-height: 10vh;
    display: grid;
    grid-auto-flow: column;
    grid-gap: 5vh;
  }

  header h1 {
    display: grid;
    justify-content: start;
    align-content: center;
    margin-left: 10vw;
    grid-auto-flow: column;
  }
  
  header nav {
    display: grid;
    justify-content: end;
    grid-auto-flow: column;
    align-content: center;
    grid-gap: 1rem;
    margin-right: 10vw;
  }

          .content-wrapper {
          display: grid;
          grid-template-columns:
            1fr
            min(65ch, 100%)
            1fr;
        }
        .content-wrapper > * {
          grid-column: 2;
        }
        .full-bleed {
          width: 100%;
          grid-column: 1 / 4;
        }

ul.posts {
    margin: 1rem;
    padding: 0;
    list-style: none;
    margin-top: 100px;
}

li.post {
    margin-bottom: 20px;
}

main p {
  margin: 1rem;
}

.post-title img {
  vertical-align: middle;
}

div.comment {
  font-size: smaller;
  margin-left: 40px;
  border-left: 1px solid;
}
div.comment p {
  margin-top: 0
}




  </style>

</head>
  <body class="h-card">
    <header>
        <h1><a class="u-url u-uid" href="/">
        <span class="u-author p-name">gzn</span></a></h1>
        <nav>
            <a href="/contact">kontakt</a>
        </nav>
    </header>

    <main class="content-wrapper">
      {{ content }}
      
      <ul class="posts">
      {% for post in collections.posts reversed %}
        <li class="post h-entry">
          
          <span class="post-title">
            {% if post.data.externalLink %}
              <img src="{{ post.data.favicon }}" width="18" /> <a class="external u-like-of u-bookmark-of" href="{{ post.data.externalLink }}" target="__blank">{{ post.data.title }}</a> - <span class="dt-published post-date">
            {{ post.date | date: "%b %d, %Y" }}
          </span>
              <div class="comment e-content">{{ post.templateContent }}</div>
            {% else %}
             <a class="self u-url" href="{{ post.url }}">{{ post.data.title }}</a> - <span class="post-date">
            {{ post.date | date: "%b %d, %Y" }}
          </span>
            {% endif %}
          </span> 
          
        </li>
      {% endfor %}
    </ul>
    </main>
  </body>
</html>
